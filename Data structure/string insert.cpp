#include<bits/stdc++.h>
using namespace std;

void inser(char old[],int p,char nw[])
{
    cout<<old;

    int len=strlen(nw);
    cout<<len;






}
int main()
{
    int pos,le=100;
    char a[200],b[100];
    cin>>a;
    cin>>pos;
    scanf("%s",&b);
      le=strlen(b);
      cout<<le;

    inser(a,pos,b);






}
